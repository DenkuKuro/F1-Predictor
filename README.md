Formula One Predictor game

**TO START DEV ENV**

in root run ```npm i```

go to /frontend ```cd frontend``` and run ```npm i```

```cd ../```

then go to /backend ```cd backend``` and run ```python -m venv venv```

```cd ../```

then go to root and run ```npm run dev```

**Remember to get environment keys**